#!/usr/bin/env python
# -*- coding: utf-8 -*-

from turtle import *

length = 100
shape('turtle')
forward(length)
right(90)
forward(length)
right(90)
forward(length)
right(90)
forward(length)
right(90)
done()
